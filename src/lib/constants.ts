export const SWIPE_THRESHOLDS = {
  OFFSET: 50,
  VELOCITY: 500,
  DEBOUNCE: 300,
} as const;

export const ANIMATION_DURATIONS = {
  CARD_TRANSITION: 300,
  SCROLL_TO_CATEGORY: 300,
} as const;

export const MAX_EVENTS = 2;

export const MOBILE_BREAKPOINT = 768;